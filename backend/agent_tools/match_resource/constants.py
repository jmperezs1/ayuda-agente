"""Row caps and text limits for `match_resource`."""

# A donor decides between a handful of places, not a ranking
DEFAULT_LIMIT = 8
MAX_LIMIT = 300
# Enough of the original wording to judge a case, not enough to quote a whole post
NOTE_CHARS = 160
